---
id: l82
order: 82
num: "82"
title: "82. Что такое нейронная сеть"
short: "Что такое нейронная сеть"
---
# 82. Что такое нейронная сеть

## Зачем это нужно

Нейронные сети лежат в основе современного AI: распознавание речи, перевод, генерация изображений, ChatGPT. Понять базовую идею нейросети — значит получить ключ к пониманию всего этого. Это не магия, а математика: числа, умножения, активации.

## Простая интуиция

Представьте детектор спама. Одно правило — «если есть слово БЕСПЛАТНО — спам» — часто ошибается. А если взять 100 слабых правил и объединить их с правильными весами — получается надёжный детектор. Нейронная сеть — это тысячи таких «правил» в слоях, каждое из которых обнаруживает свой паттерн.

## Что это такое

- **Нейрон** — вычислительная единица: принимает числа, умножает на веса, суммирует, применяет нелинейную функцию
- **Веса (weights)** — числа, которые сеть учит в процессе обучения
- **Слой (layer)** — группа нейронов, обрабатывающих одни и те же входные данные
- **Архитектура**: Input layer → Hidden layers → Output layer
- **Активационная функция** — нелинейность после суммирования (ReLU, sigmoid, tanh)
- **Backpropagation** — алгоритм обновления весов на основе ошибки

## Как это работает

```python
import numpy as np

# Один нейрон вручную
def neuron(inputs, weights, bias):
    """Линейная комбинация + активация"""
    z = np.dot(inputs, weights) + bias  # взвешенная сумма
    activation = max(0, z)              # ReLU: max(0, x)
    return activation

# Пример: нейрон с 3 входами
inputs = np.array([0.5, 0.3, 0.8])
weights = np.array([0.4, -0.2, 0.7])
bias = 0.1

output = neuron(inputs, weights, bias)
print(f"Выход нейрона: {output:.4f}")

# Простая нейросеть с numpy
def relu(x): return np.maximum(0, x)
def sigmoid(x): return 1 / (1 + np.exp(-x))

# Случайные веса (в реальности — обученные)
W1 = np.random.randn(4, 3)  # 4 нейрона, 3 входа
b1 = np.zeros(4)
W2 = np.random.randn(1, 4)  # 1 выход
b2 = np.zeros(1)

x = np.array([1.0, 2.0, 3.0])  # входные данные

# Forward pass
h = relu(W1 @ x + b1)    # скрытый слой
output = sigmoid(W2 @ h + b2)  # выходной слой
print(f"Предсказание сети: {output[0]:.4f}")
```

## Простой пример

Простая нейросеть для предсказания стоимости квартиры: входной слой с 5 нейронами (площадь, этаж, район, год постройки, число комнат), один скрытый слой с 16 нейронами (ReLU), выходной нейрон (цена). Всего: 5×16 + 16 + 16×1 + 1 = 113 обучаемых параметров. На датасете из 2 000 квартир сеть обучается за 200 эпох. Каждый нейрон в скрытом слое «специализируется»: один реагирует на сочетание «большая площадь + центральный район», другой — на «новый дом + высокий этаж».

## Практика

1. Из чего состоит один нейрон? Какие вычисления он выполняет и какова роль функции активации?
2. Что такое backpropagation и как он связан с обновлением весов нейросети?
3. Вы строите нейросеть для классификации изображений котов и собак. Сколько нейронов должно быть в выходном слое и какая функция активации подходит для последнего слоя?
4. Из урока 48 мы знаем, что параметры модели — это то, что обучается из данных. Чем «веса» нейросети (параметры) отличаются от гиперпараметров нейросети (количество слоёв, learning rate)?
5. Из урока 81 мы знаем, что ключевое отличие DL — автоматическое извлечение признаков. Как нелинейные активационные функции (ReLU) в нескольких слоях позволяют сети создавать иерархические представления данных?

## Краткий итог

- Нейрон = взвешенная сумма входов + нелинейная активация
- Несколько слоёв нейронов = нейронная сеть
- Нелинейные активации (ReLU) позволяют выучить сложные зависимости
- Обучение = оптимизация весов через backpropagation + gradient descent

## Ключевые термины урока

- **Нейрон** — базовый элемент: взвешенная сумма входов, пропущенная через активационную функцию.
- **Веса (weights)** — числовые параметры нейрона, подбираемые в процессе обучения.
- **Слой (layer)** — набор нейронов, обрабатывающих данные параллельно.
- **Активационная функция** — нелинейное преобразование после взвешенной суммы (ReLU, sigmoid).
- **Backpropagation** — алгоритм вычисления градиентов для обновления весов.